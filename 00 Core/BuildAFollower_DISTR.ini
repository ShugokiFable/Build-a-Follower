; Build a Follower — SPID (optional polish)
; Scoped to our faction only. Does NOT inject worldspace or leveled lists.
; Requires Spell Perk Item Distributor (SPID).

; Ensure templates stay in PotentialFollowerFaction for NFF / vanilla Follow me
; (already on base NPC records; this is a belt-and-suspenders runtime re-apply)
; Faction = 0x5C84E|ActorTypeNPC|0x14F73~BuildAFollower.esp||||100
; Left commented — base records already carry PotentialFollowerFaction + CurrentFollowerFaction.
; Uncomment only if another mod strips those factions from our templates.
